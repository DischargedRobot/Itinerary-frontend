interface Props {
    children: React.ReactNode
}

const PersonalLayout = (props: Props) => {

    const { 
        children
    } = props

    return (
        children
    )
}
export default PersonalLayout